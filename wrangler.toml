# wrangler.toml — Cloudflare Pages konfiguration
#
# KV namespace ID läggs INTE här — det kopplas i Cloudflare-dashboarden:
#   Pages → ditt projekt → Settings → Functions → KV namespace bindings
#   Variable name: TD_DATA  →  välj ditt namespace
#
# Inga nycklar eller ID:n hamnar i GitHub på det sättet.

name = "tournamentdirector-live"
compatibility_date = "2024-01-01"